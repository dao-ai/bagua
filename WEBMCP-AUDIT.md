# WebMCP 就绪度审计：八卦入门 · Bagua

> 审计日期：2026-06-22 | 代理：Agentic Search Optimizer

## 总体任务完成率

| 任务流 | 可发现 | 可启动 | 可完成 | 断点 | 优先级 |
|--------|--------|--------|--------|------|--------|
| 数字起卦 ($3\to 8$) | ✅ 是 | ✅ 是 | ✅ 是 | — | **已通过** |
| 金钱起卦 (6次抛铜钱) | ⚠️ 部分 | ⚠️ 部分 | ⚠️ 部分 | 需要6次点击交互 | P2 |
| AI 解卦 | ✅ 是 | ❌ 否 | ❌ 否 | 需 DeepSeek API Key | P1 |
| 本命卦生成 | ✅ 是 | ✅ 是 | ⚠️ 部分 | select 元素无障碍 | P2 |
| 64卦查询 | ✅ 是 | ✅ 是 | ✅ 是 | — | 已通过 |
| 变爻模拟 | ✅ 是 | ⚠️ 部分 | ⚠️ 部分 | 自定义 JS 动画 | P3 |
| 双卦对比 | ✅ 是 | ⚠️ 部分 | ⚠️ 部分 | 下拉选择 | P3 |
| 每日一签 | ✅ 是 | ✅ 是 | ✅ 是 | — | 已通过 |

**当前任务完成率**: 4/8 (50%)
**目标 (14天内)**: 7/8 (87.5%)

---

## Agent 摩擦力地图

### 任务流 1：数字起卦 `/divine` ✅ **已完成**

**实测结果：通过**

| 步骤 | 操作 | 状态 | 备注 |
|------|------|------|------|
| 1 | 导航到 `/divine` | ✅ | 页面正常加载 |
| 2 | 点击"🔢 数字起卦"按钮 | ✅ | React 状态切换 |
| 3 | 填写三个数字输入框 | ✅ | 需要 `type` 而非 `fill` 才能触发 React onChange |
| 4 | 点击"☰ 起 卦"按钮 | ✅ | 生成卦象 |
| 5 | 显示本卦/变卦/爻辞 | ✅ | 包含卦辞、象辞、现代释义 |
| 6 | 自动保存到历史 | ✅ | 显示"已保存到历史记录" |

**发现的问题**:
- React 受控组件需要原生 `input` 事件触发，`fill` API 不触发 React onChange
- 起卦结果没有 `data-mcp-*` 属性，Agent 无法结构化读取结果

### 任务流 2：金钱起卦 `/divine` ⚠️ **部分通过**

| 步骤 | 操作 | 状态 | 备注 |
|------|------|------|------|
| 1 | 导航到 `/divine` | ✅ | — |
| 2 | 点击"抛第一爻" | ✅ | — |
| 3 | 重复抛 6 次 | ⚠️ | 需要循环点击，容错性低 |
| 4 | 自动生成结果 | ✅ | — |

**发现的问题**:
- 需要连续 6 次交互（抛铜钱），Agent 可能在中途放弃
- 铜钱 SVG 动画是 CSS driven，Agent 无法感知动画完成状态
- 无 WebMCP 声明来告知 Agent 这是"正在进行的 6 步流程"

### 任务流 3：AI 解卦 `/ai-reading` ❌ **失败**

| 步骤 | 操作 | 状态 | 备注 |
|------|------|------|------|
| 1 | 导航到 `/ai-reading` | ✅ | — |
| 2 | 配置 DeepSeek API Key | ❌ **阻塞** | Agent 无法自行获取有效 API Key |
| 3 | 点击"选择要解读的卦" | ✅ | 弹出搜索模态框 |
| 4 | 搜索/选择卦象 | ✅ | 64卦列表可见、可点击 |
| 5 | 点击"AI 解卦"按钮 | ❌ | 按钮 disabled，因没有 API Key |
| 6 | 阅读 AI 解读 | ❌ | 无法执行 |

**关键问题**: 这是 **Agent 敌对模式 — 外部 API Key 要求**。
- API Key 输入框无 `aria-label` 和 `data-mcp-*` 声明
- 按钮 disabled 状态没有 WebMCP 解释
- 建议：添加 local LLM fallback 或预设 demo key

### 任务流 4：本命卦生成 `/lifegua` ⚠️ **部分通过**

| 步骤 | 操作 | 状态 | 备注 |
|------|------|------|------|
| 1 | 导航到 `/lifegua` | ✅ | — |
| 2 | 选择性别 | ✅ | 按钮交互 |
| 3 | 选择年月日时 (4个 `<select>`) | ✅ | 标准 HTML select，Agent 友好 |
| 4 | 点击"☰ 推算本命卦" | ✅ | — |
| 5 | 显示四柱/本命卦结果 | ✅ | 结构化内容 |

**发现的问题**:
- 结果区无 `data-mcp-*` 属性，Agent 无法提取四柱数据
- 占卜记录保存到 localStorage，Agent 跨 session 无法访问

### 任务流 5：64卦查询 `/hexagrams` ✅ **已通过**

可直接搜索/浏览/点击查看详情。无需表单提交，纯展示页面。

---

## WebMCP 发现端点状态

- **`/mcp-actions.json`**: ❌ 不存在
- **`<link rel="mcp-actions">`**: ❌ `<head>` 中未添加
- **`data-mcp-action` 属性**: ❌ 零个页面使用

---

## Agent 敌对模式清单

| 模式 | 位置 | 严重程度 |
|------|------|---------|
| 🔴 外部 API Key 要求 | `/ai-reading` — DeepSeek Key 是硬前提 | **高** |
| 🟡 React SPA 受控组件 | 所有表单 — `fill` API 不触发 onChange | **中** |
| 🟡 自定义 JS 模态/选卦弹窗 | `/ai-reading` — 没有 `aria-modal` 声明 | **中** |
| 🟢 无 `aria-label` 的输入框 | `/ai-reading` 的 API Key 输入 | **低** |
| 🟢 无 WebMCP 的结果展示 | 所有结果区，Agent 无法提取结构化数据 | **低** |

---

## 实施计划

### Phase 1 — 声明式 WebMCP (立即执行，零风险)

**目标**: 给所有表单加上 `data-mcp-*` 属性

1. `/app/divine/page.tsx` — 数字起卦表单 + 金钱起卦交互
2. `/app/ai-reading/page.tsx` — API Key 输入 + 选卦 + 解卦按钮
3. `/app/lifegua/page.tsx` — 性别选择 + 4个 select + 提交按钮
4. Layout — 导航栏链接

### Phase 2 — 发现端点 (今天)

1. 创建 `/public/mcp-actions.json`
2. 在 `layout.tsx` 中添加 `<link rel="mcp-actions">`

### Phase 3 — 敌对模式修复 (后续)

1. 给 API Key 输入框加 `aria-label` 和 `data-mcp-description`
2. 添加 disabled 按钮的 WebMCP 解释
3. 给选卦模态框加 `aria-modal` 和 `role="dialog"`
